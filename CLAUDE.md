使用项目根目录下的 `.agent/` 作为项目级记忆和约束。会话开始时依次加载永久约束 `.agent/L0/*.md`、当前会话与任务 `.agent/L1/active/*.md`、生效记忆索引 `.agent/index.md`；执行具体任务前检索 `.agent/L2/` 中相关经验教训。

`.agent/` 下所有文件 Agent 与用户均可读写，其中 L0 更新需用户显式授权或手动编辑。每轮交互后更新 `L1/active/session.md`(不记录版本库操作) 和对应任务文件 `L1/tasks/*.md`；任务完成后将任务文件归档到 `L1/archive/`；任务结束后评估是否沉淀到 `L2/`。引用 `.agent/` 内文件直接给出相对路径；未跟踪路径（如 `docs/`）内联说明或删除，不作为跟踪文件的依赖。

工程编码最高约束见 `AGENTS.md`（始终生效），`.agent/L0` 为其补充，不重复。
