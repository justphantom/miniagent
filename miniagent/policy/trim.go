package policy

// TrimForHistory trims a tool result down to limit chars before it enters history.
// Implementation kept in policy (estimate.go); this is its thin companion for callers
// referencing trim semantics directly.
