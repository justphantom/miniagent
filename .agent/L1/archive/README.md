# archive — 已归档任务

完成的任务文件从 `../tasks/`（或 `../active/`）移入此目录。文件名建议带日期前缀：`YYYY-MM-DD-<slug>.md`，frontmatter 保留 `layer: L1`、`type: task`、`updated`。

归档后评估是否将可复用经验沉淀到 `../../L2/`（`patterns/` / `decisions/` / `incidents/`）。
